This directory contains scripts that are not intended to be run in this IDE. They run in a windows subsystem for linux, where they can take advantage of GNU parallel processing of CPUs

Note to self: The linux virtual environment is loaded by calling: source ~/melts_env/venv/bin/activate

Python environment here is loaded with: ~\anaconda3\envs\torch-env