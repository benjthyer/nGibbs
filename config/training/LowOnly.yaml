tarname: '102CrGeneral' #(Suffix: '_Train', '_Test', '_Valid') - Base name for training, test, and validation datasets. Only _Train and _Test are loaded. 

warm_start: None

encoderLayerUp: 1
encoderLayerDown: 0
middleLayerUp: 2
middleLayerDown: 1
low_regularization: batchnormdropout0
high_regularization: batchnormdropout0
activation_leak: 0.05
lowWD: 1E-5
#highWD: 1E-5
#noise: 0.02 # 2%
description: ""

#tune1:
#  strategy: lower # Train encoder and saturation predictor, all else frozen. Upper portion of model not used at all for efficiency
#  batch_size: 1024 
#  epochs: 7
#  learning_rate: 5e-4
#  early_stopping_patience: 2
#  max_N: 5E5
#  tune_params: # These go in the specified order
#    low_regularization: [dropout0, batchnormdropout0, batchnorm]
#    lowWD: [0, 1e-6, 1e-5, 1e-4]
#    noise: [0, 0.01, 0.05]
#    encoderLayer: [[1,0], [1,1], [2,2], [3,2]]

train1: # Increase number for another layer of training
  strategy: lower # Train encoder and saturation predictor, all else frozen. Upper portion of model not used at all for efficiency
  batch_size: 1024 
  epochs: 50
  learning_rate: 5e-4
  early_stopping_patience: 25
  scheduler:
    type: CosineAnnealingLR # Any scheduler in optim.lr_scheduler, e.g. 'CosineAnnealingLR', 'ReduceLROnPlateau', 'CosineAnnealingWarmRestarts'.
    args:
      T_max: 10 # Then args to pass to the scheduler, e.g. factor, patience, T_0, etc. See PyTorch documentation for details.
      
  max_N: 5E5 # none, 0 and inf all mean 'no limit'
  DictFilePath: 102CrBinariesCs