alphamelts calculations
Verify totals = bulk composition given in bulk within ~1%. Use real sum and not input! 
Verify MELTS table is expected size given MELTS_indices. 
Verify that components sum to 1. 
Verify that columns match those implied by MELTS_indices objects when appending to existing array to avoid mixing data. 1st time only, when checking if os.path.exists()
VERIFY THAT BULK DOES NOT CHANGE DURING BATCH CALCS, AND THAT IT IT DOES WHEN SOLIDS CRYSTALIZE DURING FXTAL CALCS
Test that fo2 calculations based on buffer equal the absolute f02

Data Processing
Verify that the headings are identical between training and test datasets
Verify before saving memmaps that the system's sum is 100. 
